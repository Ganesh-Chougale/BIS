package com.example.bis.filter

/**
 * Color filter modes for the output window
 */
enum class ColorFilterMode {
    NORMAL,      // No filter
    INVERSE,     // Inverted colors
    MONOCHROME   // Black and white (grayscale)
}